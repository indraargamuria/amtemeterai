## Milestone: UX Enhancements & SAP Invoice Sync Extensions

### Work Package 1: Delivery Receive Page UI Improvements
- [X] **WP1.1 (Visibility Toggle):** Default view shows ONLY `Received` input. Add a toggle/button next to "Apply to All" labeled `Return/Reject`. When enabled, display all 3 inputs (`Received`, `Returned`, `Rejected`).
- [X] **WP1.2 (Page i18n):** Implement a language switcher toggle on the Delivery Receive page supporting Bahasa Indonesia and English.

### Work Package 2: Invoices Module & API Extensions
- [ ] **WP2.1 (Schema & Table Updates):** Add `BaseAmountLocal`, `BaseAmountForeign`, `DownPayAmountLocal`, and `DownPayAmountForeign` columns to the Invoices table. Re-label existing `AmountLocal` and `AmountForeign` to represent Nett Amount. Default `BaseAmount` to match `Amount` during SAP billing sync when `DownPay` is 0.
- [ ] **WP2.2 (DownPay API):** Create an API endpoint for Invoices to update `DownPay` (Local and Foreign) and automatically recalculate the Nett `Amount`.
- [ ] **WP2.3 (Compliance Category):** Update the non-delivery invoice creation API to accept `OTHER` as a valid compliance category.

### Work Package 3: Document Hub Enhancement
- [ ] **WP3.1 (Document Linkage & Details):** Upgrade Document Hub to list compiled documents cross-linked by Invoice or Delivery Order (DO). Display Customer Info, DO Received Date (if applicable), and Local/Foreign Amounts.
- [ ] **WP3.2 (Export & Email Actions):** Add buttons to:
  - Print all related documents as a single merged PDF.
  - Download/print all related documents separately.
  - Trigger an Email draft modal (Outlook/Gmail style) pre-populated with default Subject, Body, and attached documents.