# Task 1
On function UploadInvoicePrintoutInternal in InvoicesController.cs, modify OCR logic
If word "Notes" found, use "Notes", if word "Notes" not found, then search for "Remarks", so it should be sequential

# Task 2
For UploadInvoicePrintoutInternal, the OCR logic, if the found word is "Remarks", the stamp position is a bit different if the found word is "Notes", the stamp should be 0.5 cm to the bottom and 1 cm to the left, convert the position according to the existing measurement

# Task 3
For non BC delivery, the generate SAP invoice button should be visible even though the delivery is not yet confirmed