# Role
You are an expert Enterprise Systems Engineer and Senior .NET Developer. Temporarily disable the cleanup mechanism for the local development environment so we can inspect where physical files are being saved, without modifying production execution tracks.

## Context Exploration
- Read `backend-summary.md` first to grasp the entire local environment, Docker network structure, and service interactions.

## Task 1: Environment-Aware Bypass in Cleanup Block
- Open `Services/PeruriOnPremiseStampService.cs`.
- Navigate to the `finally` block at the bottom of the stamping method where the **ZERO-FOOTPRINT TRANSIENT WORKSPACE CLEANUP** happens.
- Wrap both the file-deletion loop and the subdirectory purging logic inside an environment conditional block so it only cleans up if the app is **NOT** in development mode:

  ```csharp
  finally
  {
      // =================================================================
      // ZERO-FOOTPRINT TRANSIENT WORKSPACE CLEANUP (Skipped in Dev for Debugging)
      // =================================================================
      if (_webEnv.IsDevelopment())
      {
          _logger.LogInformation("DEBUGGING: Local development mode detected. SKIPPING file and subdirectory cleanup to preserve workspace states.");
      }
      else
      {
          _logger.LogInformation("PHASE 7: Zero-footprint cleanup - Clearing temporary workspace contents");

          // Clean individual generated files first
          foreach (var file in transientFiles)
          {
              try
              {
                  if (File.Exists(file))
                  {
                      File.Delete(file);
                  }
              }
              catch (Exception ex)
              {
                  _logger.LogWarning(ex, "Failed to delete transient file: {Path}", file);
              }
          }

          // Safely delete contents of the subdirectories
          try
          {
              if (Directory.Exists(unsignedDirPath)) Directory.Delete(unsignedDirPath, true);
              if (Directory.Exists(stampDirPath)) Directory.Delete(stampDirPath, true);
              if (Directory.Exists(signedDirPath)) Directory.Delete(signedDirPath, true);
              _logger.LogInformation("PHASE 7 Complete: Subdirectories cleanly flushed.");
          }
          catch (Exception ex)
          {
              _logger.LogError(ex, "Fallback directory clearing failed.");
          }
      }
  }