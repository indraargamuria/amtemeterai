# Role
You are an expert Enterprise Systems Engineer and Senior .NET Developer. Refactor `PeruriOnPremiseStampService.cs` to dynamically toggle between Local Development (`npm run dev`) and Containerized Production environments for both file paths and network endpoints.

## Task 1: Inspect Current Configuration and Setup Environment Check
- Open `Services/PeruriOnPremiseStampService.cs`.
- Inject `IWebHostEnvironment webEnv` into the service constructor alongside your existing dependencies (`IOptions<PeruriOptions>`, `ILogger`, etc.) and store it in a private read-only field: `private readonly IWebHostEnvironment _webEnv;`.

## Task 2: Dynamically Resolve Peruri Adapter Base URL
- Locate where the HTTP Client target URL is evaluated within the stamping workflow methods.
- Refactor the code block to adapt dynamically based on environment execution states:
  ```csharp
  string peruriBaseUrl;
  if (_webEnv.IsDevelopment())
  {
      // Local development environment lookup targeting host machine exposed ports
      peruriBaseUrl = "http://localhost:9999";
  }
  else
  {
      // Containerized network landscape utilizing internal bridge network DNS
      peruriBaseUrl = "http://signadapter:7777";
  }
Update your HttpClient instance or request factory logic to cleanly utilize this dynamic peruriBaseUrl.

## Task 3: Dynamically Resolve Shared Folder Paths
Locate the initialization variables defining path markers like sharedRoot (e.g., currently locked to /app/sharefolder).

Refactor the path resolution to toggle between your physical machine directory structure and the container mount point automatically:

C#
string sharedRoot;
if (_webEnv.IsDevelopment())
{
    // Local development fallback pointing to your physical project root workspace directory
    sharedRoot = Path.Combine(Directory.GetCurrentDirectory(), "sharefolder");
}
else
{
    // Standard Docker volume mount target path
    sharedRoot = "/app/sharefolder";
}
Ensure the downstream subdirectory factories (Path.Combine(sharedRoot, "unsigned"), etc.) safely consume this dynamic base root path without changes.

Ensure that if running locally, the service checks and creates the physical local folder if missing (Directory.CreateDirectory(sharedRoot)).