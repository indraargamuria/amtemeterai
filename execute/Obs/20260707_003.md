# Role
You are an expert Frontend Engineer and UI/UX Designer specializing in high-density, enterprise-grade layouts using React, Tailwind CSS, and shadcn/ui. Refactor the `InvoicesPage.tsx` interface to optimize screen real estate, eliminate row wrapping, and implement horizontal toggle filters.

## Task 1: Context & Layout Inspection
- Analyze `frontend-summary.md` and `InvoicesPage.tsx` to understand the current grid structure, table columns, and status mappings.
- Acknowledge when done. Do not perform any code modifications yet.

## Task 2: High-Density Table Refactoring
- **Remove Index/Serial Column:** Completely remove the auto-incrementing serial number column (`No.`, `index`, or row counts) from both the table header and body rows to free up horizontal width.
- **Enforce No-Wrap Constraints:** - Apply `whitespace-nowrap` to all table cells (`<th>` and `<td>`).
  - Use structured utility classes (such as `overflow-hidden text-ellipsis`) on text properties like Customer Name or Document Numbers to prevent layout breaks on standard desktop monitors.
  - Optimize layout padding (e.g., reduce tracking layout sizes to `px-2 py-1.5` or `text-xs/sm`) to guarantee everything renders on a single uniform row.

## Task 3: Implement Horizontal Filter Toggles
- **Design Layout Requirement:** Directly above the data grid—matching the segment control layout pattern seen in the target design reference—introduce a clean, compact inline horizontal filter bar.
- **Filter Segments:** - **Compliance / Type Toggle:** Grouped buttons to toggle across `All`, `BC`, and `NonBC` configurations.
  - **Billing Status Toggle:** Quick segment selectors filtering by current invoice state values present in your records (`All`, `Draft`, `SyncedToSap`, `Stamped`, `Voided`).
- **State Integration:**
  - Connect these layout toggle elements directly into your client-side `useMemo` filtering pipeline so that they compound seamlessly with your existing top-bar text search.
  - Apply sharp, minimal background status fills (e.g., state accents with subtle text colors) to represent active selections clearly.