# Role
You are an expert Enterprise Systems Engineer and Senior Full-Stack Developer. Update the OpexNOW invoicing integration engine to handle the updated SAP ERP response schema, update the database models with Entity Framework Core, and align the frontend user interface.

## Task 1: Context Gathering & Codebase Inspection
- Read `frontend-summary.md` and `backend-summary.md` to establish accurate relational mappings between `DeliveryHeader` and `Invoice` entities.
- Skip if you already understand the context, Acknowledge when done. Do not perform any modifications yet.

## Task 2: Database Schema & Migration updates
- **Update Invoice Model (`Models/Invoice.cs`):**
  - Deprecate or remove the single old `Amount` column.
  - Add `decimal AmountForeign` (Decimal 18,2).
  - Add `decimal AmountLocal` (Decimal 18,2).
  - Add `string Currency` (Required, Max Length 10, e.g., "USD", "IDR").
  - Add `string ComplianceCategory` or `string IntegrationType` to store the "BC" or "NonBC" classification string.

## Task 3: Backend API Integration Refactoring
- Locate the target endpoint handler for: `POST /api/deliveries/{deliveryNumber}/invoice`
- **Modify the SAP Response Parsing DTO:**
  - Update the internal JSON deserialization DTO to match the updated SAP payload contract:
    - `sapInvoiceNumber` (string)
    - `billingDate` (DateTime)
    - `amountForeign` (decimal)
    - `amountLocal` (decimal)
    - `currency` (string)
    - `customerNumber` (string)
    - `customerName` (string)
    - `poNumber` (string)
    - `deliveryNumber` (string)
    - `MESSAGE` (string)
    - The structural `BC` / `NonBC` indicator node.
- **Update Mapping & Persistence Logic:**
  - Extract the data from the newly added fields (`amountForeign`, `amountLocal`, `currency`, and the BC/NonBC classification node).
  - Map and save these properties directly into the corresponding columns in the `Invoice` table record during creation.
  - Maintain the existing guardrails added in Task 5 of the billing workflow configuration (e.g., locking out `BillingBlocked` states).
  - Ensure compilation succeeds without breaking downstream code dependencies.

## Task 4: Frontend UI Alignment
- **Update TypeScript Interfaces:**
  - Open your frontend API type contracts file and update the `Invoice` model interface to replace the old `amount` property with `amountForeign`, `amountLocal`, `currency`, and the new classification type property.
- **Refactor `InvoicesPage.tsx` Layout:**
  - Update the table grid tracking to replace the single total column with clean, high-density enterprise accounting columns:
    - **Local Amount:** Formatted accurately using local currency metrics (e.g., using `Rp.` or the dynamic `currency` state indicator).
    - **Foreign Amount:** Displayed adjacent to or inline with the local total if foreign exchange applies (e.g., `USD 5,334.50`).
    - **Classification Tag:** Render a dense, distinctive badge showing whether the transaction falls under the `BC` or `NonBC` compliance category.
  - Verify that sorting and filtering controls function as intended with the updated dataset properties.