# Role
You are an expert Frontend Engineer specializing in high-density Enterprise SaaS UI architecture. Refactor the `InvoicesPage.tsx` view to handle conditionally responsive accounting layout formats, implement unified filter toggles and export layouts, and introduce client-side Excel reporting functionality.

## Task 0: Context & Layout Inspection
- Analyze `frontend-summary.md`, `DeliveriesPage.tsx` and `InvoicesPage.tsx` to understand the current grid structure, table columns, and status mappings.
- Acknowledge when done. Do not perform any code modifications yet.

## Task 1: Clean Alignment of Control Elements
- **Layout Alignment:**
  - Place the newly added segment filter toggles (Compliance and Billing Status) and the "Export to Excel" control element on a single inline header row exactly above the invoices table grid layout.
  - Apply the exact design accents, compact padding (`px-3 py-1.5`), and borders used on the deliveries page export button to make sure the interface elements are uniform.

## Task 2: Conditional Currency Column Logic
- **Display Design Strategy:**
  - Audit the table layout loop mapping over invoice records.
  - **Condition A (Local Currency e.g., IDR):** If the invoice record `currency` field is `"IDR"`, render only **one single bold amount value** cleanly aligned to the right (e.g., `Rp 88.803.422,00`), omitting any redundant foreign currency fields.
  - **Condition B (Foreign Currency e.g., USD):** If the record contains a foreign currency value, preserve a balanced dual-line layout context (e.g., displaying the local total `Rp 88.803.422,00` as primary text and the original foreign amount `USD 5,334.50` inside a muted sub-text element beneath it).
  - Ensure all layout structures utilize `whitespace-nowrap` to prevent text wrapping on standard desktop views.

## Task 3: Client-Side Excel Export Integration
- **Export Functionality:**
  - Add a "Export to Excel" action button utilizing `xlsx` (SheetJS) matching the operational logic built into the deliveries page.
  - Target exclusively the *currently active filtered dataset* state tracking variable.
  - **Data Schema Mapping:** Map raw operational entity fields into structured enterprise reporting header keys:
    - `Invoice Number`
    - `Billing Date`
    - `Customer Name`
    - `Currency`
    - `Local Amount`
    - `Foreign Amount` (Default to 0 or null if transaction is IDR-native)
    - `Type` (BC / NonBC)
    - `Status`
  - Append an automated date stamp parameter to the filename layout (e.g., `OpexNOW_Invoices_Report_YYYY-MM-DD.xlsx`).