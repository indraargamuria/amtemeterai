You are an expert Enterprise DevOps Engineer and Solutions Architect. We are preparing to deploy our system, OpexNOW (also referred to as AmtemeterAI in legacy configurations), for a production go-live. 

**CRITICAL MANDATE:** The absolute core objective of this exercise is to eliminate all hardcoded endpoints, secret keys, URLs, and credentials from the source files, centralizing them exclusively into standard runtime configurations (`appsettings.json` and `.env`). This refactor MUST NOT change any underlying application logic, APIs, or business processes. All existing C# Option class structures, internal container networking dependencies, and application behaviors must remain perfectly intact and working without issue.

We will execute this deployment audit in sequential tasks. Do not attempt all tasks at once. Wait for my confirmation after each task before moving to the next.

---

### PRE-TASK: Documentation Verification & Sync
Before we begin the configuration generation, you need to read and analyze two specific source files present in our current project context: `backend-summary.md` and `frontend-summary.md`. 

Please perform a thorough review of both files to identify all integrations, API endpoints, SAP configurations, third-party credentials, email settings, database parameters, storage setups, and environment switches mentioned across both architectures.

**Your Action:** Reply acknowledging that you have read both `backend-summary.md` and `frontend-summary.md`. Summarize in 2–3 sentences what you understand to be the primary tech stack and integration components of the system. Do not generate the configuration files yet.

---

### TASK 1: Unified Project Root `.env` Template
*(To be executed after confirmation of Pre-Task)*
Generate a clean, well-commented production `.env` file grouping all variables logically (Database, JWT, SAP, Peruri, MinIO, SMTP, Google Maps, App URLs). 
- Ensure EVERY single API endpoint, token, and credential found in both summary files is explicitly accounted for so that nothing remains hardcoded in code files.
- Replace all staging/development placeholders or hardcoded dev values with empty production injection placeholders (e.g., `YOUR_PRODUCTION_DB_PASSWORD`).
- Use ASP.NET Core double-underscore (`__`) naming conventions where appropriate to ensure seamless environment variable mapping to backend configuration classes.

---

### TASK 2: Backend `appsettings.json` Structure
*(To be executed after Task 1)*
Provide the corresponding structured JSON configuration layout mapping directly to the C# configuration option classes (`SapOptions`, `PeruriOptions`, `MinioOptions`, `SmtpSettings`, `JwtOptions`). 
- Ensure arrays like `Cors:Origins` are properly mapped.
- Maintain identical structure and naming keys to cleanly bind with the environment variables defined in Task 1, ensuring zero friction or breakage in the existing ASP.NET Core Dependency Injection framework.

---

### TASK 3: Comprehensive Deployment Checklist
*(To be executed after Task 2)*
Provide a concise, production-ready checklist detailing runtime infrastructure dependencies to guarantee operational continuity:
- Every external URL/IP that needs network visibility/firewall rules (SAP ERP, Peruri Staging/Production Endpoints, Google Maps Geocoding API, etc.).
- Internal Docker container ports and naming dependencies (`http://signadapter:7777`, `minio:9000`, `postgres:5432`, etc.).
- A list of the seed accounts that must be immediately changed or disabled post-migration (e.g., `admin@amtemeterai.com`).
- Critical file paths/volume allocations required for execution (`/app/sharefolder` named volume layout).