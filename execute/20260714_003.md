You are an expert Enterprise Systems Engineer and C# Backend Developer. We are executing a production migration for OpexNOW. 

**YOUR EXACT MISSION:** 
You must explicitly analyze, identify, and refactor any hardcoded configuration string, API endpoint, credentials, connection string, or secret key located inside the specific files listed below. You will replace them entirely with dynamic configuration binding via ASP.NET Core's `IOptions<T>` or `IConfiguration` patterns, referencing our centralized `.env` and `appsettings.json` keys.

*Architectural Freedom:* If you identify that the current `Config/` directory is missing dedicated configuration classes required to cleanly eliminate hardcoding (for instance, missing `MinioOptions.cs`, `JwtOptions.cs`, or `ConnectionStrings.cs`), you are explicitly expected to generate these additional configuration files to make the project architecture robust and complete.

---

### PRE-TASK: Documentation & Project Tree Verification
Before writing the refactored code, you need to read and analyze two source context files present in our workspace: `backend-summary.md` and `frontend-summary.md`.

**Your Action:** Reply acknowledging you have read both files. Give a bulleted list predicting which specific classes from our target structure (listed below) will contain hardcoded URLs (such as SAP connections, Peruri links, or SMTP credentials). Do not generate code yet.

---

### MAIN REFACTORING TASK: Targeted Code Modifications
Once cleared to proceed, you will systematically rewrite the code for the following target project files to ensure zero hardcoded properties remain:

#### 1. Configuration Layer (`Config/` Folder)
Refactor existing classes or generate new strongly-typed options properties as needed:
- `Config/PeruriOptions.cs`
- `Config/SapOptions.cs`
- `Config/SmtpSettings.cs`
- *(Generate any additional needed classes here, e.g., MinioOptions.cs, JwtOptions.cs, etc.)*

#### 2. Startup Integration (`Program.cs`)
- Provide the exact initialization logic inside `Program.cs`.
- Bind configuration sections seamlessly from `appsettings.json` / environment variables into the DI container using `builder.Services.Configure<T>()`.
- Eliminate any hardcoded paths, CORS string arrays, or database connection strings here.

#### 3. Core Domain Services (`Services/` Folder)
Inject the proper `IOptions<T>` dependencies into the constructors and replace legacy hardcoded strings inside the implementation methods for:
- `Services/EmailService.cs` (utilizing `SmtpSettings`)
- `Services/ErpCustomerSource.cs` (utilizing `SapOptions`)
- `Services/MinioStorageService.cs`
- `Services/PeruriPdsService.cs` (utilizing `PeruriOptions`)
- `Services/PeruriOnPremiseStampService.cs`
- `Services/PeruriSessionService.cs`
- `Services/BillingBackgroundService.cs` & `Services/Backup.cs` (checking for environment/path configurations)

#### 4. Web API Layer (`Controllers/` Folder)
Ensure no controllers bypass the options pattern or hardcode mock client routes. Audit and refactor:
- `Controllers/AccountController.cs`
- `Controllers/CustomersController.cs`
- `Controllers/DashboardController.cs`
- `Controllers/DeliveriesController.cs`
- `Controllers/InvoicesController.cs`
- `Controllers/SapSimulationController.cs`
- `Controllers/TestController.cs`
- `Controllers/UserManagementController.cs`

**Execution Guardrail:** Ensure that all existing method signatures, interface implementations, and internal logic workflows remain completely functional with zero architectural breaking changes.