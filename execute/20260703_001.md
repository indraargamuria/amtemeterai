You are an expert Frontend Engineer specializing in high-density, minimalist Enterprise SaaS dashboards using React, Tailwind CSS, and shadcn/ui. 

Your task is to implement two specific features in our Deliveries module by first understanding the codebase architecture through our documentation.

### 1. Context Gathering
Please read and analyze the following two files to understand our current state, component hierarchies, and design language:
- `frontend-summary.md`
- `backend-summary.md`

### 2. Implementation Goals
Modify and improve the existing `DeliveriesPage.tsx` layout to seamlessly incorporate the following:

#### Feature A: Advanced Filtering (Delivery & Invoice Status)
- Integrate two new dropdown/select filters alongside our existing "Search", "Compliance Type", and "Discrepancy" controls.
- **Filter 1 (Delivery Status):** Options for All, Fully Received, Partial Received, and Canceled.
- **Filter 2 (Invoice Status):** Options for All, Invoiced, and Pending Invoice.
- Update the client-side `useMemo` filtering pipeline so that all filters compound correctly without breaking existing logic.
- Ensure the layout remains clean, high-density, and compact—do not wrap rows awkwardly on standard desktop screens.

#### Feature B: Client-Side Excel Export
- Install/import `xlsx` (SheetJS) to handle the export completely on the client side.
- Add an "Export to Excel" action button in the control bar (match our clean, minimal slate UI with a subtle emerald accent or icon).
- The export must dynamically pull the *currently filtered dataset* (`filteredDeliveries`).
- Map and format the raw data keys into clean, human-readable business headers (e.g., "Delivery Number", "Date", "Customer Name", "Status", "Invoiced") and cleanly format dates to local string formats.

### 3. Requirements
- **Maintain Design Language:** Adhere to our high-density, enterprise-level layout rules. Use concise padding, clear typography, and subtle borders.
- **Type Safety:** Ensure all newly introduced state variables and mapped export structures are fully typed with TypeScript.
- **Incremental Diff:** Provide the complete file updates or clean, precise code blocks showing exactly where to insert the new state, helper functions, and JSX layout modifications.

Let me know once you've reviewed the markdown summaries, and provide the exact implementation code.